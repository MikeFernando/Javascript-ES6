// Retorne o url da página atual utilizando o objeto window
const href = document.location.href
alert(href)

// Seleciona o primeiro elemento da página que possua a classe ativo
const firstClassActive = document.querySelector('.ativo')
console.log(firstClassActive)

// Retorne a linguagem do navegador
const languageBrowser = navigator.language
console.log(languageBrowser)

// Retorne a largura da janela
const widthWindow = innerWidth
console.log(widthWindow)

